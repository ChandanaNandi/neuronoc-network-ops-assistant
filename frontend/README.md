# NeuroNOC frontend

Vite + React + TypeScript. Phase 1 scope: a dashboard shell only — no API calls, no live status, no routing.

## Setup

```bash
pnpm install
```

## Dev server

```bash
pnpm dev
```

Runs on `http://localhost:5173`.

## Build

```bash
pnpm build
```
